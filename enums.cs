using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Template 
{
    public enum LayoutType
    {
        control,
        mutation
    }
    public enum Colors
    {
        Red = 0,
        Green = 1
    }
    public enum TargetType
    {
        undefined = 0,
        bacteria = 1,
        Parasite = 2
    }
}
